<Page x:Class="OpenCC.NET.GUI.Views.TextPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:viewModels="clr-namespace:OpenCC.NET.GUI.ViewModels"
      Title="TextPage">

    <Page.DataContext>
        <viewModels:TextViewModel />
    </Page.DataContext>
    <Grid>
        <GroupBox Header="文本編輯">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="12,12,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="原始文本" VerticalAlignment="Center" FontSize="16"/>
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button Command="{Binding PasteCommand}" ToolTip="貼上" Margin="0,0,5,0">
                                <ui:FontIcon Glyph="&#xE77F;"/>
                            </Button>
                            <Button Command="{Binding ClearCommand}" ToolTip="清空">
                                <ui:FontIcon Glyph="&#xE74D;"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                    <TextBox
                        Grid.Row="1"
                        Margin="12,5,12,12"
                        ui:ControlHelper.PlaceholderText="請輸入文本"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        Text="{Binding OriginalText, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="12,12,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="轉換結果"
                            VerticalAlignment="Center"
                            FontSize="16" />
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button Command="{Binding CopyConvertedCommand}" ToolTip="複製" Margin="0,0,5,0">
                                <ui:FontIcon Glyph="&#xE8C8;"/>
                            </Button>
                            <Button Command="{Binding ClearConvertedCommand}" ToolTip="清空">
                                <ui:FontIcon Glyph="&#xE74D;"/>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <TextBox
                        Grid.Row="1"
                        Margin="12,5,12,12"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        Text="{Binding ConvertedText, Mode=OneWay}"
                        IsReadOnly="True" />
                </Grid>
            </Grid>
        </GroupBox>
    </Grid>
</Page>
